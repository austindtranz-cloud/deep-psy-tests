# Бэклог задач (TODO)

## Технический долг и Безопасность (Security)
- [ ] **Privacy (Очистка данных)**: Добавить кнопку «Удалить мои данные» (очистка `localStorage`) внутри нового глобального меню/настроек, когда оно будет спроектировано. *Данные не должны удаляться автоматически.*
- [ ] **Защита от XSS**: Добавить функцию `escapeHTML()` и обернуть в неё вывод всех текстов вопросов (`q.text`) и описаний (`test.intro`) перед рендером через `innerHTML`. (А лучше использовать `DOMPurify`, если будут внедряться поля открытого ввода пользователем).
- [ ] **Защита API Интеграций**: В будущем (на этапе бэкенда) настроить отправку PDF (email) через прокси-функцию сервера, чтобы не светить API-ключи на фронтенде.

## Интерфейс и Дизайн
- [ ] **Глобальное Меню (Бургер / Настройки)**: Разработать UI/UX глобального меню дашборда, где будут располагаться настройки пользователя, юридическая информация и кнопка удаления данных.
